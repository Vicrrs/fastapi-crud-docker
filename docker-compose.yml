services:
  web:
    build: .
    command: python run.py
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - .env
  db:
    image: postgres:16.1
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
